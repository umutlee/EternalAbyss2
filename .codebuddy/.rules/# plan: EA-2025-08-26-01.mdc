## **1\) 修測試：ConfigManager API（消 `GetConfig` 參數數量不符 與 `ConfigName()` 調用）**

**檔案**：`Buildings/Tests/BuildingManagerConfigTest.cs`

**變更**：

* `GetConfig<T>("name")` → **新版** `GetConfig<T>()`

* `ConfigName()` → **屬性** `ConfigName`

`@@`  
`-            var config = configManager.GetConfig<BuildingConfigSO>("BuildingConfig");`  
`-            Assert.IsNotNull(config, "BuildingConfig should be loaded");`  
`-            Assert.AreEqual("BuildingConfig", config.ConfigName());`  
`+            var config = configManager.GetConfig<BuildingConfigSO>();`  
`+            Assert.IsNotNull(config);`  
`+            Assert.AreEqual("BuildingConfig", config.ConfigName);`

---

## **2\) ServiceManager：Reverse() 回傳 void（消 CS0815）**

**檔案**：`Core/Services/ServiceManager.cs`

`@@`  
`-            var sortedServices = SortServicesByDependencies().Reverse();`  
`+            using System.Linq;`  
`+            var sortedServices = SortServicesByDependencies().AsEnumerable().Reverse();`

`List<T>.Reverse()` 是**就地反轉、回傳 void**；用 `AsEnumerable().Reverse()` 拿到 `IEnumerable<T>`。

---

## **3\) TerrainQueryService：介面 vs 具體型別（消「ITerrainChunk → TerrainChunk」）**

**檔案**：`Terrain/Services/TerrainQueryService.cs`

### **(a) 取塊函式（第 59 行附近）**

`-        if (_terrainChunks.TryGetValue(chunkCoord, out ITerrainChunk chunk) && chunk is ITerrainChunk terrainChunk)`  
`+        if (_terrainChunks.TryGetValue(chunkCoord, out ITerrainChunk chunk) && chunk is DeepAbyssHive.Terrain.Data.TerrainChunk terrainChunk)`  
         `{`  
             `return terrainChunk;`  
         `}`

### **(b) 取鄰近塊（第 457 行附近，兩處）**

`-            if (_terrainChunks.TryGetValue(coord, out ITerrainChunk chunk) && chunk is ITerrainChunk terrainChunk)`  
`+            if (_terrainChunks.TryGetValue(coord, out ITerrainChunk chunk) && chunk is DeepAbyssHive.Terrain.Data.TerrainChunk terrainChunk)`  
             `{`  
                 `chunks.Add(terrainChunk);`  
             `}`

重點：回傳型別是 `TerrainChunk`（struct），pattern matching 必須對**具體型別**做判斷。

---

## **4\) SpatialIndexManager.API：把 `List<object>` 轉回 `List<SpatialNode>`（消多個 CS0029）**

**檔案**：`SpatialIndex/Managers/SpatialIndexManager.API.cs`

專案裡已有 `ListConvert_Compat.cs` 的 `ToSpatialNodes()`。

**四處改成** `return result.ToSpatialNodes();`

`@@ // 194 附近`  
`-            return result;`  
`+            return result.ToSpatialNodes();`

`@@ // 198 附近`  
`-            return result;`  
`+            return result.ToSpatialNodes();`

`@@ // 237 附近`  
`-            return result;`  
`+            return result.ToSpatialNodes();`

`@@ // 241 附近`  
`-            return result;`  
`+            return result.ToSpatialNodes();`

---

## **5\) BuildingQueryService：補遺失 helper ＆ 修錯誤成員名（消多個 `GetBuildingPlayerId` 與 `buildingType`）**

**檔案**：`Buildings/Services/BuildingQueryService.cs`

**新增**（類內任一 private 區塊）：

`private static int GetBuildingPlayerId(BuildingData b) => b.OwnerId;`

**替換所有** `buildingType` → `BuildingType`（struct 現有的是屬性 `BuildingType`／或 `Type`，依你的檔案，錯誤是在第 240 行先修那一處）

`-    if (b.buildingType == someType)`  
`+    if (b.BuildingType == someType)`

---

## **6\) CreepGridService：`Bounds` → `Vector3` 參數型別（消兩個 Bounds 轉 Vector3）**

**檔案**：`Creep/Services/CreepGridService.cs`

（第 104、126 行附近）

`-    _spatialIndex.Insert(creepObj, creep.Position, creep.Bounds);`  
`+    _spatialIndex.Insert(creepObj, creep.Position, creep.Bounds.size);`

`-    _spatialIndex.Update(creepObj, creep.Position, creep.Bounds);`  
`+    _spatialIndex.Update(creepObj, creep.Position, creep.Bounds.size);`

這組 API 的第三參數是**尺寸**（`Vector3 size`），不是 `Bounds`。

---

## **7\) CreepSourceType 命名空間錯誤（消 `CreepSource_Compat.cs` 的 CS0234）**

你的工作樹裡 `Creep/Compat/CreepSource_Compat.cs` 目前引用 `DeepAbyssHive.Creep.Enums.CreepSourceType`，但 enum 定義在 `DeepAbyssHive.Creep.Data`。

**檔案**：`Creep/Compat/CreepSource_Compat.cs`

`-using DeepAbyssHive.Creep.Enums;`  
`+using DeepAbyssHive.Creep.Data;`

若此檔不在你的 zip，但出現在本機及錯誤清單，直接改 `using` 即可過。

---

## **8\) Unit 音效欄位與播放（消兩個 string→AudioClip）**

你之前也提到需要把 `UnitData` 增加欄位。建議資料層**只存路徑字串**，播放時再載入 `AudioClip`（struct/NativeArray 友善）。

### **(a) 加欄位（消 `MaxEnergy/AttackSound/DeathSound` 不存在 與 音效型別不符）**

**檔案**：`Units/Data/UnitData.cs`

`public struct UnitData`  
 `{`  
`+    public float MaxEnergy;          // 新增`  
`+    public string AttackSound;       // 新增：Resources 路徑`  
`+    public string DeathSound;        // 新增：Resources 路徑`  
     `// …其餘既有欄位…`  
 `}`

### **(b) 播放端載入 `AudioClip`**

**檔案**：`Units/Core/Unit.cs`（第 377 與 424 行附近）

`-    if (_audioSource != null && _unitData?.AttackSound != null)`  
`-        _audioSource.PlayOneShot(_unitData.AttackSound);`  
`+    if (_audioSource != null && !string.IsNullOrEmpty(_unitData.AttackSound))`  
`+    {`  
`+        var clip = Resources.Load<AudioClip>(_unitData.AttackSound);`  
`+        if (clip != null) _audioSource.PlayOneShot(clip);`  
`+    }`

`-    if (_audioSource != null && _unitData?.DeathSound != null)`  
`-        _audioSource.PlayOneShot(_unitData.DeathSound);`  
`+    if (_audioSource != null && !string.IsNullOrEmpty(_unitData.DeathSound))`  
`+    {`  
`+        var clip = Resources.Load<AudioClip>(_unitData.DeathSound);`  
`+        if (clip != null) _audioSource.PlayOneShot(clip);`  
`+    }`

這兩處同時解決：`string → AudioClip` 型別錯、以及 `_unitData?.X` 在 struct 的不當 null-條件存取。
