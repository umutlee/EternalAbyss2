---
description: 
globs:
alwaysApply: true
---



# **MVP Definition of Done（更新版）**

1. 進入 `Dev_Playground` 可連續運行 ≥10 分鐘、Console 無未處理例外、記憶體無線性上升。

2. 單張地圖：有可查詢的地形（先平地→可換分塊）、能生成 **creep source**，creep 會擴張/衰減。

3. 互動：Pause/Resume、×1/×2/×4 並全系統遵守同一時間源。

4. 存讀：最小世界狀態（creep grid+sources+時間倍率）可 Save/Load（帶版本號）。

5. 最低 UI/Overlay：時間倍率、簡易資源占位、Chunk/Creep 可視化開關。

---

## **基線（M0）— 啟動/場景/煙霧測試**

* ✅ EA-M0-T01 Boot：`BootEnsureManagers` 自動建立 `Managers` 並掛上 `CreepManager / UnitManager / SpatialIndexManager`。

* ✅ EA-M0-T02 Dev 場景：`Dev_Playground` 已能 Play；`BuildingPlacer` 可用（B 鍵）。

* ✅ EA-M0-T03 冒煙：`RuntimeSmoke` 能檢到 `CreepManager`。

後續任務都假設以上為已成立的穩定基線。

---

## **M1 — Terrain 基礎（資料層為主，先平地，留接口）**

⬜️ **EA-M1-T01 Grid & Provider 接線**  
 **目的**：統一世界↔格點換算，讓 TerrainQuery 有資料來源。  
 **交付**：`GridUtil`（CellSize=2f）；`ITerrainProvider` \+ `FlatTerrainProvider`（高度=0、Type=Ground）；`TerrainQueryService` 接到 Provider。  
 **驗收**：在 Dev 場景中呼叫 `SampleHeight/Type` 有穩定回傳；Grid 往返誤差 \< 1e-4。

⬜️ **EA-M1-T02 TerrainConfigSO & ApplyConfig**  
 **目的**：把 chunkSize/maxLOD/viewDistance 先落在 ScriptableObject，供後續擴展。  
 **交付**：`TerrainConfigSO`（chunkSize/maxLOD/viewDistance…）；`ITerrainManager.ApplyConfig()` 樣板。  
 **驗收**：Inspector 改值能被 Manager 讀到（暫不重建 Mesh）。

⬜️ **EA-M1-T03 Terrain Overlay v0（輕量調試）**  
 **目的**：低成本顯示格線/邊界，驗證格點與查詢一致。  
 **交付**：F3 開關；以 Gizmos/GL.LINES 繪 Grid（抽樣）。  
 **驗收**：開/關對 FPS 影響 \< 5%。

備註：真正的 Chunk/LOD Streamer 放到「M1+」之後再切；先確保 creep 能運轉。

---

## **M2 — Creep 核心（Grid / Source / Simulation / Query / Overlay）**

⬜️ **EA-M2-T01 枚舉與資料殼**  
 **目的**：補齊先前報錯用到的枚舉/欄位。  
 **交付**：`CreepSourceType{Manual,Basic,Enhanced,Specialized}`、`CreepTileType`、`CreepTileStatus`；`CreepData / CreepSource / CreepNetworkInfo`。  
 **驗收**：專案內無再出現這些型別缺失。

⬜️ **EA-M2-T02 CreepConfigSO & Defaults**  
 **目的**：把數值調成資產。  
 **交付**：`CreepConfigSO`（growRate/decayRate/sigma/tickInterval/densityThreshold/gridW/H），Resources 下預設一份。  
 **驗收**：Play 後可讀到預設數值。

⬜️ **EA-M2-T03 CreepGridService**  
 **目的**：集中管理密度網格。  
 **交付**：`Initialize(w,h)`；`Set/Get/TryGet/Update()`；內部 `CreepData[,]`。  
 **驗收**：啟動時初始化 128×128，不丟例外。

⬜️ **EA-M2-T04 CreepSourceService**  
 **目的**：管理來源清單。  
 **交付**：`Add/Remove/Get`；List 持有；在 `CreepManager.Start()` 放 1 顆預設 source。  
 **驗收**：能在 Console 列出 1 個有效來源。

⬜️ **EA-M2-T05 CreepSimulationService v1**  
 **目的**：最小可收斂的擴張/衰減。  
 **交付**：每 `tickInterval` 對網格做：  
 `d += grow*(1-d)*exp(-r/sigma) - decay*d`（夾到 0..1）。  
 **驗收**：30 秒內可見密度前線外擴；停掉 source 後會後退。

⬜️ **EA-M2-T06 CreepQueryService \+ 放置整合**  
 **目的**：遊戲互動入口。  
 **交付**：`IsCreep(worldPos)`／`DistanceToFrontier()`；`BuildingPlacer` 增 `RequireCreep` 勾選，預覽顏色依據 `IsCreep`。  
 **驗收**：勾選後只有 creep 範圍內可放置。

⬜️ **EA-M2-T07 Creep Overlay v1**  
 **目的**：觀察密度與前線。  
 **交付**：Gizmos 按密度（抽樣）著色；可顯示最近前線距離。  
 **驗收**：Overlay 與 Query 結果一致。

⬜️ **EA-M2-T08 CreepStatistics v1（1Hz）**  
 **目的**：即時面積/前線長度統計，供 UI。  
 **交付**：`ICreepStatisticsService.Get()`；Manager 每秒更新一次。  
 **驗收**：Dev HUD 顯示數值、Pause 時停止變化。

---


new M3
**M3：建築放置與空間檢查（與菌毯耦合）** ⏩ 進行中  
- M03-01：**minSpacing 規則**（本 MDC）。  
- M03-02：**SpatialIndex 最近鄰優化**（可選）：  
  - 若 `SpatialIndex` 支援最近鄰查詢，就把 `ViolatesMinSpacing` 換成「查最近距離 < minSpacing → collision」。  
- M03-03：**Grid Snap（可選）**：  
  - 在 `BuildingPlacer` 增加 `snapSize`（GameConfig 外放），預覽與實際放置共用。  
- M03-04：**預覽顏色與錯誤碼提示**（已具備基礎，強化 UI 文案即可）。  
- M03-05：**SMOKE**：  
  - 放置在 creep 外、壓建築、壓邊界、距離不足 → 分別回 `E_REQUIRE_CREEP / E_PLACE_COLLISION / E_OUT_OF_BOUNDS / E_PLACE_COLLISION`。

--
old M3
## **M3 — 遊戲循環 / UI / 存讀 / 穩定化**

⬜️ **EA-M3-T01 TimeService \+ Pause/×1×2×4**  
 **目的**：統一時間源。  
 **交付**：`ITimeService`（Delta/ScaledDelta/IsPaused/Scale）；快捷鍵 Space/1/2/3。  
 **驗收**：倍率切換後 creep 速度線性變化；Pause 完全停止。

⬜️ **EA-M3-T02 最低 HUD & Overlay 控制**  
 **目的**：基本操作面。  
 **交付**：時間倍率顯示、資源占位（BioEnergy/Population）、Overlay 開關。  
 **驗收**：UI 互動無 GC 峰值，FPS 穩定。

⬜️ **EA-M3-T03 Save/Load v1（版本化）**  
 **目的**：能關掉再進還原世界。  
 **交付**：`SaveService`（`meta.json` \+ `world.json`）；存 sources、grid 密度（可 RLE）、時間倍率；`version="v1"`。  
 **驗收**：Stop→Play→Load 後狀態一致；留 `migrate(v0→v1)` 空殼。

⬜️ **EA-M3-T04 啟動順序與護欄**  
 **目的**：避免重入、殘留協程。  
 **交付**：Managers 初始化旗標（`isInitialized/isDisposed`）、OnDisable/OnDestroy 退訂；高風險步驟外圈 try/catch 記 Warn。  
 **驗收**：連續進出 Play 多次，無重覆初始化/殘留警告。

⬜️ **EA-M3-T05 Scenario Loader（可選但推薦）**  
 **目的**：可一鍵載入測試場景片段。  
 **交付**：`assets/scenarios/*.json`（空白/壓力/擴張壅塞）。  
 **驗收**：三個樣本可載入、5 分鐘內 FPS 穩。

---

## **M4 — 穩定化與護欄（與日後 CI 接軌）**

⬜️ **EA-M4-T01 記憶體/事件退訂規範**（長時間運行檢查）  
 ⬜️ **EA-M4-T02 微效能**：tick 節流、批次處理、物件池，降低 GC。  
 ⬜️ **EA-M4-T03 Headless 冒煙**：3 分鐘自動跑、輸出指標/截圖。  
 ⬜️ **EA-M4-T04 決定論/回放殼**：固定種子 \+ 指令錄影重演。  
 ⬜️ **EA-M4-T05 資產/序列化驗證**：開機快掃，錯就阻擋進場（Dev）。  
 ⬜️ **EA-M4-T06 第三方隔離**：RTS Engine 改名 `RTS Engine~` 或 asmdef 僅 Editor，避免 Runtime 初始化。  
 ⬜️ **EA-M4-T07 文件**：README/Runbook/QA 指南。

---

## **PR 流程與命名（保持節奏）**

* **分支**：`feat/ea-m{milestone}-t{nn}-{slug}`（例：`feat/ea-m2-t05-creep-sim-v1`）

* **PR 大小**：每 PR 1–3 檔、≤200 行；附「變更摘要 / 驗收步驟 / 風險」。

* **優先序（現在就做）**：

  1. **EA-M1-T01** Grid & Provider →

  2. **EA-M2-T01/T02**（枚舉+Config）→

  3. **EA-M2-T03/T04/T05**（Grid/Source/Sim）→

  4. **EA-M2-T06/T07**（Query+Overlay）→

  5. **EA-M3-T01**（時間服務）→ **EA-M3-T03**（存讀）。

---
