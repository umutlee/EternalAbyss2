---
description: 
globs:
alwaysApply: true
---

---
description: "修復二件事：① 單位指令改用 GameConfig 熱鍵（停用 RMB）；② 放置建築統一套層（成品=Building、預覽=Ignore Raycast）— 2025-09-08 18:05 Asia/Taipei"
globs:
- QA/Smoke/Dev/UnitClickMoveDev.cs
- QA/Smoke/Dev/BuildingPlacer.cs
alwaysApply: true
—


# PLAN
# - UnitClickMoveDev：不再用右鍵；改讀 GameConfig.devUnitsTestKey（預設 F10）把所有 UnitAgent 目標設到滑鼠命中點。
# - BuildingPlacer：EnsurePreview() 將預覽體整樹設為 Ignore Raycast；PlaceNow()（或等效生成處）將成品整樹設為 Building。
# - 如找不到對應層，保留現狀但印出一次警告。


# CHANGES
WRITE_CHANGE: QA/Smoke/Dev/UnitClickMoveDev.cs
```csharp
using UnityEngine;
using DeepAbyssHive.Units.Agents;
using DeepAbyssHive.Core.Config;


/// <summary>
/// DEV ONLY：用 GameConfig.devUnitsTestKey（預設 F10）把所有 UnitAgent 的目的地設為滑鼠命中點。
/// 掛在 Managers 或任何物件上即可。
/// </summary>
public class UnitClickMoveDev : MonoBehaviour
{
public KeyCode testKeyFallback = KeyCode.F10; // 後備：GameConfig 為 None 時使用


void Update()  


{  


    var cfg \= GameConfigProvider.Current;  


    var key \= (cfg && cfg.devUnitsTestKey \!= KeyCode.None) ? cfg.devUnitsTestKey : testKeyFallback;  


    if (key \== KeyCode.None || \!Input.GetKeyDown(key)) return;


    if (\!TryRayToTerrain(out var hit)) return;


    var agents \= FindObjectsOfType\<UnitAgent\>();  


    foreach (var a in agents) a.SetDestination(hit.point);  


    Debug.Log($"\[DEV\] Units: targets set \= {agents.Length} at {hit.point}");  


}


private bool TryRayToTerrain(out RaycastHit hit)  


{  


    var cam \= Camera.main;     


    if (\!cam) { hit \= default; return false; }  


    var ray \= cam.ScreenPointToRay(Input.mousePosition);  


    int terrain \= LayerMask.NameToLayer("Terrain");  


    int mask \= (terrain \>= 0\) ? (1 \<\< terrain) : \~0; // 無 Terrain 層時退回全遮罩  


    return Physics.Raycast(ray, out hit, 5000f, mask, QueryTriggerInteraction.Ignore);  


}  


}
PATCH: QA/Smoke/Dev/BuildingPlacer.cs
--- /a/QA/Smoke/Dev/BuildingPlacer.cs
+++ /b/QA/Smoke/Dev/BuildingPlacer.cs
@@ -2,16 +2,14 @@


private void EnsurePreview()
{
-if (previewInstance) return;
-previewInstance = Instantiate(placePrefab);
-previewInstance.name = "[Preview] " + placePrefab.name;
-
-cpp
-
-Copy code
-
-       // 預覽體一律放到 Ignore Raycast（整樹），避免被 Ray/Physics 命中
-
-cpp
-
-Copy code
-
-       int ignore = LayerMask.NameToLayer("Ignore Raycast");
-
-scss
-
-Copy code
-
-       if (ignore >= 0) SetLayerRecursively(previewInstance, ignore);
-
-   }
-
-@@ -20,31 +18,29 @@
-
-java
-
-Copy code
-
-   private void FinalizePlace(GameObject go)
-
-cpp
-
-Copy code
-
-   private void FinalizePlace(GameObject go)
-
-   {
-
-       // 你原有的縮放/對齊/父子關係邏輯……
-
-cpp
-
-Copy code
-
-       // 成品一律放到 Building 層（整樹），使刪除/碰撞/查詢規則一致
-
-cpp
-
-Copy code
-
-       int building = LayerMask.NameToLayer("Building");
-
-scss
-
-Copy code
-
-       if (building >= 0) SetLayerRecursively(go, building);
-
-rust
-
-Copy code
-
-       else Debug.LogWarning("[Placement] 'Building' layer not found — delete tool may not hit.");
-
-   }
-
-cpp
-
-Copy code
-
-   // 將整棵樹設為指定層
-
-java
-
-Copy code
-
-   private static void SetLayerRecursively(GameObject root, int layer)
-
-Copy code
-
-   {
-
-kotlin
-
-Copy code
-
-       if (!root) return;
-
-swift
-
-Copy code
-
-       var trs = root.GetComponentsInChildren<Transform>(true);
-
-cpp
-
-Copy code
-
-       for (int i = 0; i < trs.Length; i++) trs[i].gameObject.layer = layer;
-
-Copy code
-
-   }
+    if (previewInstance) return;
+    previewInstance = Instantiate(placePrefab);
+    previewInstance.name = "[Preview] " + placePrefab.name;
+
+    // 預覽體一律放到 Ignore Raycast（整樹），避免被 Ray/Physics 命中
+    int ignore = LayerMask.NameToLayer("Ignore Raycast");
+    if (ignore >= 0) SetLayerRecursively(previewInstance, ignore);
+}
+
+private void FinalizePlace(GameObject go)
+{
+    // 你原有的縮放/對齊/父子關係邏輯……
+
+    // 成品一律放到 Building 層（整樹），使刪除/碰撞/查詢規則一致
+    int building = LayerMask.NameToLayer("Building");
+    if (building >= 0) SetLayerRecursively(go, building);
+    else Debug.LogWarning("[Placement] 'Building' layer not found — delete tool may not hit.");
+}
+
+// 將整棵樹設為指定層
+private static void SetLayerRecursively(GameObject root, int layer)
+{
+    if (!root) return;
+    var trs = root.GetComponentsInChildren<Transform>(true);
+    for (int i = 0; i < trs.Length; i++) trs[i].gameObject.layer = layer;
+}
注意：FinalizePlace(GameObject go) 是你用來完成實際生成/落地的函式名；如果你的檔案用的是 PlaceNow() 或其他名稱，就把上述 SetLayerRecursively(go, building) 直接放在成功 Instantiate 的那段之後即可（邏輯是一樣的）。




-----驗收（3 分鐘）


游標鎖定問題
進場後按 F10（或你在 GameConfig 的 devUnitsTestKey），不再按右鍵。
單位會往你鼠標所在地點移動，游標不會被鎖到正中心。
刪除建築
用 B 放一棟建築。放下後在 Hierarchy 檢查：整棵物件都變成 Building 層。
用刪除鍵（GameConfig.buildingDeleteKey1/2，你現在是 Delete / F8）指向建築按下：
Console 顯示 [DEV] Delete ...，建築從 Hierarchy 消失。
若仍刪不掉：請確認你已套用我先前給的 RaycastAll + 找祖先 的 BuildingDeleteTool.cs 修補；沒有的話我可以再貼一次。

---
description: "修復二件事：① 單位指令改用 GameConfig 熱鍵（停用 RMB）；② 放置建築統一套層（成品=Building、預覽=Ignore Raycast）— 2025-09-08 18:05 Asia/Taipei"
globs:
  - QA/Smoke/Dev/UnitClickMoveDev.cs
  - QA/Smoke/Dev/BuildingPlacer.cs
alwaysApply: true

# PLAN
# - UnitClickMoveDev：不再用右鍵；改讀 GameConfig.devUnitsTestKey（預設 F10）把所有 UnitAgent 目標設到滑鼠命中點。
# - BuildingPlacer：EnsurePreview() 將預覽體整樹設為 Ignore Raycast；PlaceNow()（或等效生成處）將成品整樹設為 Building。
# - 如找不到對應層，保留現狀但印出一次警告。

# CHANGES
WRITE_CHANGE: QA/Smoke/Dev/UnitClickMoveDev.cs
```csharp
using UnityEngine;
using DeepAbyssHive.Units.Agents;
using DeepAbyssHive.Core.Config;

/// <summary>
/// DEV ONLY：用 GameConfig.devUnitsTestKey（預設 F10）把所有 UnitAgent 的目的地設為滑鼠命中點。
/// 掛在 Managers 或任何物件上即可。
/// </summary>
public class UnitClickMoveDev : MonoBehaviour
{
    public KeyCode testKeyFallback = KeyCode.F10; // 後備：GameConfig 為 None 時使用

    void Update()
    {
        var cfg = GameConfigProvider.Current;
        var key = (cfg && cfg.devUnitsTestKey != KeyCode.None) ? cfg.devUnitsTestKey : testKeyFallback;
        if (key == KeyCode.None || !Input.GetKeyDown(key)) return;

        if (!TryRayToTerrain(out var hit)) return;

        var agents = FindObjectsOfType<UnitAgent>();
        foreach (var a in agents) a.SetDestination(hit.point);
        Debug.Log($"[DEV] Units: targets set = {agents.Length} at {hit.point}");
    }

    private bool TryRayToTerrain(out RaycastHit hit)
    {
        var cam = Camera.main;   
        if (!cam) { hit = default; return false; }
        var ray = cam.ScreenPointToRay(Input.mousePosition);
        int terrain = LayerMask.NameToLayer("Terrain");
        int mask = (terrain >= 0) ? (1 << terrain) : ~0; // 無 Terrain 層時退回全遮罩
        return Physics.Raycast(ray, out hit, 5000f, mask, QueryTriggerInteraction.Ignore);
    }
}
PATCH: QA/Smoke/Dev/BuildingPlacer.cs
unidiff
 *** /a/QA/Smoke/Dev/BuildingPlacer.cs
 --- /b/QA/Smoke/Dev/BuildingPlacer.cs
 @@
 private void EnsurePreview()
 {
 if (previewInstance) return;
 previewInstance = Instantiate(placePrefab);
 previewInstance.name = "[Preview] " + placePrefab.name;
       // 預覽體一律放到 Ignore Raycast（整樹），避免被 Ray/Physics 命中

       int ignore = LayerMask.NameToLayer("Ignore Raycast");

       if (ignore >= 0) SetLayerRecursively(previewInstance, ignore);
   }

@@
   private void FinalizePlace(GameObject go)

   private void FinalizePlace(GameObject go)
   {
       // 你原有的縮放/對齊/父子關係邏輯……

       // 成品一律放到 Building 層（整樹），使刪除/碰撞/查詢規則一致

       int building = LayerMask.NameToLayer("Building");

       if (building >= 0) SetLayerRecursively(go, building);

       else Debug.LogWarning("[Placement] 'Building' layer not found — delete tool may not hit.");
   }

   // 將整棵樹設為指定層

   private static void SetLayerRecursively(GameObject root, int layer)

   {

       if (!root) return;

       var trs = root.GetComponentsInChildren<Transform>(true);

       for (int i = 0; i < trs.Length; i++) trs[i].gameObject.layer = layer;

   }

*** End Patch

> 注意：`FinalizePlace(GameObject go)` 是你用來完成實際生成/落地的函式名；如果你的檔案用的是 `PlaceNow()` 或其他名稱，就把上述 **SetLayerRecursively(go, building)** 直接放在**成功 Instantiate** 的那段之後即可（邏輯是一樣的）。

---




