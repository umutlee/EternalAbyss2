---
description: 
globs:
alwaysApply: true
---
description: EA-2025-08-26-07 — Fix UnitData duplicates + add enum members (SpatialObjectType.All, CreepSourceType.CreepTumor)
globs:
  - Assets/DeepAbyssHive/**/*
alwaysApply: true
---

# PLAN
- 去除 `Units/Data/UnitData.cs` 內部重複欄位定義（6 個重複）。
- 為 `SpatialIndex.Enums.SpatialObjectType` 補上 `All` 成員，解除多處 `.All` 找不到。
- 為 `CreepSourceType` 補上 `CreepTumor` 成員（Enums/Data 兩處若皆存在則都補）。
- 不改動其餘行為，只做「可編譯」止血。

# CHANGES

## 1) UnitData — 移除重複欄位，統一為單一定義
# 將檔內這 6 個欄位確保「每個只出現一次」。若有第二份重複，刪除重複那份。
# 為避免破壞其他相依，暫維持 Attack/DeathSound 的型別為 string（如需播放請於用處轉成 AudioClip）。
WRITE_CHANGE: Assets/DeepAbyssHive/Units/Data/UnitData.cs
REWRITE_FIELDS_BLOCK:
<<<csharp
// --- Canonical basic fields (ensure only one of each) ---
public string UnitName;
public float  MoveSpeed;
public float  AttackCooldown;
public float  DetectionRange;

// sound ids / resource paths (keep as string for now)
public string AttackSound;
public string DeathSound;
// --- end canonical fields ---
>>>
NOTES:
- 若檔內有同名欄位（上述 6 個）出現兩次，保留本段，刪除其他重複定義。
- 其餘欄位與內容保持原樣。

## 2) SpatialObjectType — 補上 All 成員
# 檔案通常位於 Enums/SpatialObjectType.cs。若不存在請回報。
WRITE_CHANGE: Assets/DeepAbyssHive/SpatialIndex/Enums/SpatialObjectType.cs
ENSURE_MEMBER:
<<<csharp
// 在 enum SpatialObjectType 內加入（若已存在則略過）
All = -1
>>>
NOTES:
- 不更動既有成員數值；僅新增 `All = -1` 以支援「不篩選」語意。
- 若檔案缺少 `using System;` 亦無須加入 [Flags]，本次僅補常數。

## 3) CreepSourceType — 補上 CreepTumor 成員
# 兩處皆檢查：Enums 與 Data 若其中存在該 enum 就補上；若兩處都存在就兩邊都補。
WRITE_CHANGE: Assets/DeepAbyssHive/Creep/Enums/CreepSourceType.cs
ENSURE_MEMBER:
<<<csharp
// 在 enum CreepSourceType 內加入（若已存在則略過）
CreepTumor = 4
>>>

WRITE_CHANGE: Assets/DeepAbyssHive/Creep/Data/CreepSourceType.cs
ENSURE_MEMBER:
<<<csharp
// 在 enum CreepSourceType 內加入（若已存在則略過）
CreepTumor = 4
>>>

# VALIDATION
- 重新編譯後，關於：
  - `UnitData` 的 CS0102（重複定義）應消失。
  - `SpatialObjectType.All` 缺失相關錯誤應全部消失。
  - `CreepSourceType.CreepTumor` 缺失錯誤應消失。
- 若仍有「AudioClip 參數收到 string」之錯誤，之後批次將在使用端以 `Resources.Load<AudioClip>(AttackSound)` 轉換處理。
