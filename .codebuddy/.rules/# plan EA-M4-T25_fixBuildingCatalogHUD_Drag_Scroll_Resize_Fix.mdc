description: "Hotfix: 修正 RuntimeMiniConsole 的 GUI.Window 視窗 ID 無效數字常數（0xDA_MINI → 0xDA11CE）"
globs: ["Assets/DeepAbyssHive/Core/Logging/**"]
alwaysApply: true

# PLAN
- 最小修補（單一替換）：把非法的十六進位常數 `0xDA_MINI` 改為合法整數 `0xDA11CE`（da-11-ce）。
- 其它程式碼不動。

# CHANGES
FIND: "Assets/DeepAbyssHive/Core/Logging/RuntimeMiniConsole.cs"
---
0xDA_MINI
---
REPLACE: "Assets/DeepAbyssHive/Core/Logging/RuntimeMiniConsole.cs"
---
0xDA11CE
---

# 註解
- C# 的十六進位常數只能使用 0-9 與 A-F；`_MINI` 並非有效數字，觸發 `CS1013` 與 `CS1003`。
- 使用 `0xDA11CE`（讀作 da-11-ce）作為穩定 Window ID，避免與其它 GUI.Window 衝突。

# 驗收步驟
- 套用本 MDC 後重新編譯：`CS1013` / `CS1003` 應消失。
- 進 Play：`RuntimeMiniConsole` 浮窗可正常顯示/拖曳/釘選。

# 回滾
- 將 `0xDA11CE` 改回原值（不建議，會再次出錯）。
