# DeepAbyss — QA/Smoke/Dev README
> 面向開發/驗收的操作指南與約定（Managers/工具/HUD/熱鍵/Layers）。  
> 版本：2025-09-10 18:40 Asia/Taipei

---

## 0) Managers 與啟動
- Play 時會自動建立 **Managers** 根物件並 `DontDestroyOnLoad`（由 Boot 確保）。
- 找不到 Managers / ~PlacementOutline 時，可用 Editor 選單：
  - `DeepAbyss → Dev → Select Managers (DontDestroyOnLoad)`
  - `DeepAbyss → Dev → Select Placement Outline`

---

## 1) Layers 約定
- **Terrain**：地表/地形/可 Raycast 命中。
- **Building**：建築成品（`BuildingPlacer` 在「成功放置」後會把**整棵物件**切到 Building）。
- **Ignore Raycast**：預覽/臨時物（`[Preview] …` 會被設為 Ignore Raycast，避免被命中）。
- 刪除工具與碰撞檢查都以以上規則行事；Prefab 事前不必手改 Layer。

---

## 2) GameConfig（集中熱鍵與放置/單位參數）
**資源路徑**：`Assets/Resources/Configs/GameConfig.asset`  
**關鍵欄位**：
- Placement：`useSpatialIndexForPlacement` / `minSpacing` / `margin` / `requireCreep`
- UX：`snapSize` / `rotationStepDegrees`
- Dev/Testing Keys：`placementSmokeKey(F7)` / `buildingDeleteKey1(Delete)` / `buildingDeleteKey2(F8)` / `devUnitsSpawnKey(F9)` / `devUnitsTestKey(F10)` / `devSpawnCount(200)`
- Units×Creep：`creepSpeedMul` / `offCreepSpeedMul` / `creepSampleInterval`

> 進場時 Console 會印出 `[DEV HUD] Game: …` 串，含上述關鍵值（用於快速確認）。

---

## 3) Dev 工具一覽（如何使用）
### 3.1 UnitDevSpawner（**F9** 生成、**F10** 指派）
- 掛在場景任意物件（建議 **Managers**）。  
- **F9**：在滑鼠地點生成 `devSpawnCount` 隻（可不指定 prefab，會自動用 Capsule+UnitAgent 後備）。  
- **F10**：把場景所有 `UnitAgent` 的目標設為滑鼠地點。  
- Log：`[DEV] Units: spawned/targets set ...`

### 3.2 UnitClickMoveDev（驗收小工具）
- 若場景已有 `UnitDevSpawner`，本工具會自動 **muted**，避免 F10 雙觸發。  
- 否則：讀 `devUnitsTestKey`（F10）→ 指派所有 `UnitAgent` 移動到滑鼠地點。

### 3.3 CreepBrushAndRunner（手繪種子＋Runner）
- **Enable Seeding** ✔ 後，以左鍵在地面塗種子。  
- 想保持形狀：把 **Budget Per Frame = 0**（暫停擴張/退火）。  
- 想恢復擴張：把 Budget 調回原值（如 2000）。

### 3.4 CreepLinePainter（兩點成線畫菌毯）
- 左鍵一次設 **A**，再左鍵設 **B**，沿 AB 以 `radius`/`stepRatio` 撒種子。  
- `rayMask` 建議指向 **Terrain**；打不中會自動 fallback 到 `~0` 並警告一次。  
- Scene 有 Gizmos 綠球顯示落點；Log：`[DEV] CreepLinePainter: painted ...`

### 3.5 UnitCreepBinder（單位×菌毯綁定）
- 啟用時將 `UnitAgent.OnCreepPredicate = CreepManager.IsSetWorld`；找不到 CreepManager 會在稍後 **重試一次**。  
- 配合 GameConfig 的 `creepSpeedMul/offCreepSpeedMul/creepSampleInterval`，單位進入菌毯會加速並印 `UnitAgent: creep=ON/OFF`。

### 3.6 BuildingPlacer（B 鍵開/關建造）
- 預覽體自動設 **Ignore Raycast**；放置成功的成品自動設 **Building**（整樹）。  
- 驗證順序（預覽/放置一致）：`Snap → SnapRotation → Bounds(含 previewHeight 下移) → ValidateByConfig(有向) → Tint`。  
- 若 `useSpatialIndexForPlacement=true`：Physics 與 SpatialIndex 都需通過。  
- 刪除建築：見 3.7。

### 3.7 BuildingDeleteTool（刪除建築）
- 熱鍵：`buildingDeleteKey1(Delete)`、`buildingDeleteKey2(F8)`。  
- **mac 相容**：若設為 Delete，**Backspace** 亦視為有效（Editor 可能攔截 Delete，請把焦點放在 Game 視窗；F8 永遠可用）。  
- RaycastAll 命中後**向上尋找第一個 Building 層祖先**→ 刪除（忽略 `[Preview]` 與 Ignore Raycast 物件）。  
- Log：`[DEV] Delete <name>`。

---

## 4) HUD（可拖曳＋記住位置）
- 通用工具：`HudDragUtil`（以 PlayerPrefs 永續位置）。  
- 已支援可拖曳的 HUD：
  - `CreepStatsHUD`（key=`HUD.CreepStats`）
  - `TerrainStreamingDebugHUD`（Editor，key=`HUD.StreamHUD`）
  - `PlacementStatusHUD`（key=`HUD.PlaceStatus`）
  - `BuildingPlacerStatusHUD`（key=`HUD.BuildPlacer`）
- 用法：拖動標題列即可；退出 Play 再進來會保留位置。

---

## 5) SMOKE / 快速驗收
- **Placement SMOKE**：按 `placementSmokeKey`（預設 **F7**）→ Console 連續輸出 `[SMOKE] ... PASS`。  
- **單位路測**：`F9` 生成 → `CreepBrushAndRunner` 畫路或 `CreepLinePainter` A→B → `F10` 指派 → 進入菌毯印 `creep=ON`。  
- **刪除**：指向建築按 `Delete/F8` → `[DEV] Delete ...`。

---

## 6) 疑難排解（FAQ）
- **看不到 Managers**：這是 `DontDestroyOnLoad` 區塊，請用 Editor 菜單 `Select Managers` 一鍵選到。  
- **Delete 沒反應**：把焦點切到 **Game** 視窗；mac 上 `Delete` 可能等於 **Backspace**；或改用 Key2（F8）。  
- **無法刪除建築**：確認成品已在 **Building** 層（放置成功會自動切）；若是預覽體 `[Preview]` 不會刪。  
- **畫菌毯沒反應**：設定 `CreepLinePainter.rayMask=Terrain`；或看 Console 的 fallback 警告；Scene 會有綠球 Gizmos。  
- **右鍵指令導致游標鎖定**：已統一用 `devUnitsTestKey(F10)`，請勿再用 RMB 指令（避免與 Crosshair/HUD 衝突）。  
- **預覽綠但放不下**：請更新到含「縮放一致＋Y 偏移一致」的 BuildingPlacer 修補；Tint/快取與驗證順序已統一。

---

## 7) DO-NOT-TOUCH（除非本 README/patch 指定）
- `BootEnsureManagers.cs`、`PlacementValidator*.cs`、`Result<T> / PlaceResultCode`（介面已穩定）  
- `GameConfigSO.cs` 欄位名稱（回溯相容，改名需映射）。

---

## 8) 變更紀錄（要點）
- M3：Placement 校驗一致化、Delete RaycastAll、HUD 可拖曳。  
- M4：T03 路徑佇列 Runner；T04 Spawner；T05 單位×菌毯（取樣＋加速）；Creep 畫線可靠性＋Binder 重試。

> 任何新工具/HUD 請沿用此 README 的結構新增到對應章節，並保持 ≤200 行/每 PR 1–3 檔的節奏。