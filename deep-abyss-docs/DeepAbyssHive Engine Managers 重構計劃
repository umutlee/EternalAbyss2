# **DeepAbyssHive 管理器重構設計說明書**

**版本**：v1.0 **日期**：2025-08-11（台北時間）

---

## **目標與原則**

* **目標**：將 6 個 Manager（Game / Unit / Building / Terrain / Creep / SpatialIndex）拆分為可維護、可測試、可替換的模組，同時**不破壞現有 API 與場景序列化**。

* **原則**：

  1. 先**partial class 拆檔**，絕不改對外簽章；

  2. 配置資料**外置到 ScriptableObject**；

  3. 透過 **IManager / 各子系統接口**做依賴注入；

  4. **小步提交，小補丁**修錯與搬移。

---

## **目前架構快照（重點）**

* **GameManager**：MonoBehaviour，集中調度，在 `Update()` 逐一呼叫所有 IManager.Update(deltaTime) 與 FixedUpdate(fixedDeltaTime)。這是全域節點。

* **UnitManager**：非 MonoBehaviour，職責大（建立/移動/戰鬥/進化/環境適應/空間索引互動）。

* **BuildingManager**：非 MonoBehaviour，含模板/研究/建造/升級與批次更新佇列。

* **TerrainManager**：非 MonoBehaviour，分塊地形、修改佇列與歷史、查詢 API。

* **CreepManager**：MonoBehaviour，網格化菌毯、擴張/衰減、網路資料、空間索引整合。

* **SpatialIndexManager**：MonoBehaviour，包裝 Octree/QuadTree，已有無參數 Unity Update \+ 顯式 IManager.Update。

---

## **拆解策略（最佳實踐）**

### **1\) 先做 partial class（零功能變更，僅拆檔）**

不改類名、命名空間、public 簽章；MonoBehaviour 至少保留一個檔案名與類名一致。

**命名建議（每類 4–6 檔，依專案調整）：**

* **GameManager**（`MonoBehaviour`）

  * `GameManager.Core.cs`：單例/狀態/註冊/生命週期（現有 Awake/Start/Update/FixedUpdate/LateUpdate）。

  * `GameManager.Managers.cs`：註冊/反註冊/批次初始化與更新橋接（目前在 Update 中逐一呼叫 IManager）。

  * `GameManager.Perf.cs`：FPS/Query 計時、統計。

* **UnitManager**（非 `MonoBehaviour`）

  * `UnitManager.Core.cs`：資料結構、初始化/清理/暫停、Prefab 路徑初始化。

  * `UnitManager.Movement.cs`：MoveUnit / Update Moving 狀態與空間索引更新。

  * `UnitManager.Combat.cs`：AttackTarget / PerformAttack / 攻擊冷卻管理。

  * `UnitManager.Evolution.cs`：EvolveUnit / AdaptToEnvironment 與屬性修改器。

  * `UnitManager.Query.cs`：GetUnitsInRange / GetUnitsOfType 等查詢。

  * `UnitManager.View.cs`：Instantiate/Update 外觀。

* **BuildingManager**（非 `MonoBehaviour`）

  * `BuildingManager.Core.cs`：模板/研究字典、初始化/清理、基礎 CRUD。

  * `BuildingManager.Construction.cs`：建造/升級/修理流程與進度更新。

  * `BuildingManager.Research.cs`：研究/前置條件/玩家研究狀態。

  * `BuildingManager.Query.cs`：範圍查詢/類型查詢/放置驗證。

  * `BuildingManager.Updates.cs`：批次更新佇列、節流。

* **TerrainManager**（非 `MonoBehaviour`）

  * `TerrainManager.Core.cs`：塊資料、歷史、初始化/清理/更新管理。

  * `TerrainManager.Generation.cs`：噪聲生成/高度/類型判定。

  * `TerrainManager.Modify.cs`：修改佇列/套用/歷史維護。

  * `TerrainManager.Query.cs`：GetChunkAt/IsPassable/移速修正。

* **CreepManager**（`MonoBehaviour`）

  * `CreepManager.Core.cs`：網格與網路資料結構、初始化/清理、無參數 Update、顯式 IManager.Update。

  * `CreepManager.Growth.cs`：擴張/收縮/修復與密度演化。

  * `CreepManager.Query.cs`：覆蓋/強度/範圍查詢。

  * `CreepManager.Sources.cs`：源點增刪/合併/分割網路。

* **SpatialIndexManager**（`MonoBehaviour`）

  * `SpatialIndexManager.Core.cs`：索引建立/清理/批次處理協程。

  * `SpatialIndexManager.API.cs`：Add/Update/Remove/Query/Optimize/性能統計。

  * *已存在* 無參數 `Update/FixedUpdate` \+ 顯式介面實作，保持不變。

**注意**：MonoBehaviour 的主檔（`GameManager.cs` / `CreepManager.cs` / `SpatialIndexManager.cs`）必須保留與類名同名；其他 partial 檔名可自由。

---

### **2\) 配置外置：ScriptableObject（SO）**

**是否值得？** 值得。不是新潮流，是 Unity 長年推薦的資料資產化方案。好處：

* **去硬編碼**：像 Unit/Building 屬性、進化/研究/地形噪聲參數，從程式搬到 SO。UnitManager/BuildingManager 目前有大量硬編碼字典與數值。

* **可視化/版本化**：策劃可在 Inspector 編輯，Git 能審核差異。

* **快取與共享**：多場景共用同一組資料，不需重複載入。

**落地步驟（以 Unit 為例）**：

1. 建 `UnitConfigDB : ScriptableObject`，包含 `Dictionary<UnitType, UnitAttributes>`、`EvolutionPathDef[]`、`EnvironmentAdaptationDef[]`。

2. `UnitManager.Initialize()` 先讀 `UnitConfigDB`，取代硬編碼初始化（現在是 `InitializeUnitPrefabPaths()/InitializeEvolutionPaths()/InitializeEnvironmentAdaptations()`）。

3. Building/Terrain 亦同：BuildingTemplates/ResearchTemplates、Terrain 噪聲/高度/尺寸參數做成 SO。

**兼容策略**：第一階段先保留原硬編碼，新增一個「若 SO 存在則優先，用不到就 fallback 到舊邏輯」的分支，等資料建齊再移除 fallback。

---

### **3\) 邏輯解耦與服務化（第二階段以後）**

* 把查詢/命令從 Manager 中抽為服務介面（仍由 Manager 統一持有實作，以便替換/測試）：

  * `IUnitQueryService` / `IUnitCommandService`、`IBuildingConstructionService`、`ICreepSimulationService` 等。

* **GameManager** 保持「調度者」：目前設計良好，於 `Update()` 逐個 manager 調用 `Update(deltaTime)`；無需 Unity 帶參數版本（已在 GameManager 內統一處理）。

---

## **可靠性與效能備忘**

* **MonoBehaviour 的 Update/FixedUpdate**：無參數；IManager 介面版本才帶 `deltaTime`。在 Creep/SpatialIndex 這點已修正，並用 `Time.deltaTime` 於無參數版。

* **NativeArray(Allocator.Temp)**：目前 `UnitManager.GetUnitsInRange / GetUnitsOfType` 回傳 Temp 過陣列，**呼叫端要負責 Dispose**，否則可能產生 GC/記憶體壓力（規劃包一層結果容器或改用 pooled List 亦可）。

* **批次/節流**：Building 有批次更新隊列、Terrain 有修改節流、SpatialIndex 有批次佇列/協程，沿用即可；把參數（間隔/批量）外置到 SO。

* **長檔不直接拖慢執行效能**，但會讓工具與人都難維護 → 先 partial 化，再局部優化（如移除高頻 `Debug.Log`、避免 LINQ/裝箱、減少重複距離計算）。

---

## **迭代路線圖（建議 Sprint 計畫）**

**Sprint 0：止血 & 穩定**

* 修完所有「Unity 無參數 Update/FixedUpdate」相關報錯（你已修大半）。Creep/SpatialIndex 維持：無參數 Unity 版 \+ 顯式 IManager 版。

* GameManager 持續統一調用 `manager.Update(deltaTime)`（現有邏輯不動）。

**Sprint 1：partial 拆檔**

* 6 類全部 partial 化；只「搬移方法」，不改簽章與可見性。

* 建立最小單元測試：每個 Manager 的核心 API smoke test（Create/Query/Destroy 等）。

**Sprint 2：SO 外置（先讀新，後備舊）**

* 建 `UnitConfigDB/BuildingDB/TerrainConfig` SO；Initialize 時優先讀 SO。

* 建資料遷移腳本：從現有硬編碼生成 SO（一次性工具）。

**Sprint 3：查詢/命令服務化**

* 將高耦合片段抽到 Service（仍由 Manager new/注入），為後續多執行緒/Job 做鋪墊。

**Sprint 4：性能/記憶體**

* 熱路徑剖析（Profiler/Deep Profile），針對性優化（查詢濾波、距離平方比較、陣列池、移除熱路徑 log）。

---

## **檔案與模組索引（對照）**

* GameManager：集中調度、速度/時間縮放、Tick 管理、註冊流程。

* UnitManager：戰鬥/移動/進化/環境適應/空間索引交互（含多處硬編碼屬性/進化/適應表 → SO）。

* BuildingManager：建造/升級/研究/放置驗證/批次更新。

* TerrainManager：地形塊載入/生成/修改佇列/歷史/查詢。

* CreepManager：菌毯網格/擴張/衰減/網路/空間索引 \+ 無參數 Update。

* SpatialIndexManager：Octree/QuadTree 管理、批次佇列、查詢 API、效能統計 \+ 無參數 Update。

---
